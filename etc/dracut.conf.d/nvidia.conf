# Installed to /etc/dracut.conf.d/nvidia.conf by bootstrap.sh.
# Bake the NVIDIA modules into the initramfs; run `xbps-reconfigure -fa` after.
add_drivers+=" nvidia nvidia_modeset nvidia_uvm nvidia_drm "

# Minimal base. Repos + nvidia + desktop + gaming handled elsewhere.
# One package per line. '#' comments and blank lines ignored.
git
curl
chrony
# Browser: Google Chrome via Flatpak (pkgs/flatpaks.txt), not Firefox.
# (Chrome is proprietary / not in xbps; `chromium` is the native alternative.)
# cronie: cron daemon (+anacron) — fires the weekly fstrim TRIM job (docs/05).
cronie
# zramen: compressed-RAM swap manager (config: etc/sv/zramen/conf + sysctl).
zramen
# chezmoi: dotfiles manager — `chezmoi init --apply <repo>` restores your config
# on a fresh box (dotfiles live in a SEPARATE repo, not this one — see docs/05).
chezmoi

# Lean gaming — native xbps packages. Proton (bundled with Steam) covers
# DXVK/VKD3D. Faugus comes via Flatpak (pkgs/flatpaks.txt); Lutris pulls Wine +
# deps via xbps. nvidia-libs-32bit is in 30-nvidia.txt.
steam
lutris
mono
# gamemode: `gamemoderun %command%` flips the CPU governor to performance + renices
# while a game runs (works on runit via elogind D-Bus; needs the `gamemode` group).
gamemode
# MangoHud (+32bit): FPS/frametime overlay — `MANGOHUD=1 %command%`. NB: Void builds
# it without NVML, so GPU%/VRAM read 0 on NVIDIA (use nvtop alongside); FPS works.
MangoHud
MangoHud-32bit
# libunwind: external Wine / GE-Proton builds (Lutris) link libunwind.so.8 in
# their ntdll.so; the lean set won't pull it otherwise -> GE-Proton installs fail.
libunwind
libgcc-32bit
libstdc++-32bit
libdrm-32bit
libglvnd-32bit
libva-32bit
# 32-bit host audio: Lutris/Wine 32-bit prefixes + SLR-disabled native titles run
# SILENT without these (Proton's pressure-vessel bundles its own, so SLR is fine).
libpulseaudio-32bit
alsa-plugins-pulseaudio-32bit
alsa-pipewire-32bit
libopenal-32bit
SDL2-32bit

# Lean KDE Plasma 6 (Wayland). `kde-plasma` is the CURRENT metapackage
# (kde5 no longer exists). We deliberately DO NOT install kde-baseapps /
# kde-applications — that is the lean win. konsole + dolphin added by hand.
kde-plasma
sddm
konsole
dolphin
xorg-minimal
NetworkManager
pipewire
wireplumber
libspa-bluetooth
# bluez: the Bluetooth daemon (ships /etc/sv/bluetoothd, enabled in services.txt)
# + the `bluetooth` group. libspa-bluetooth above only gives PipeWire the BT
# audio backend; without bluez there is no adapter to pair anything to.
bluez
elogind
dbus
polkit
flatpak
xdg-desktop-portal-gtk
noto-fonts-ttf
noto-fonts-emoji
dejavu-fonts-ttf
